import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("This Project s a simple project!")
                .font(Font.largeTitle)
            Spacer()
        }
        
        
    }
}
